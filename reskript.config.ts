import {configure} from '@reskript/settings';

export default configure(
    'webpack',
    {
        build: {
            appTitle: 'Panda Design',
            favicon: 'favicon.svg',
        },
    }
);
